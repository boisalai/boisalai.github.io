const SAS = {
    template: `<div class="container mt-5">
        <h1>SAS</h1>
 
        <h2>OPTIONS Statement</h2>

        Specifies or changes the value of one or more SAS system options.

        <pre><code class="language-sas">OPTIONS PAGESIZE=51 LINESIZE=200;</code></pre>

        <h2>LIBNAME Statement</h2>

        Associates or disassociates a SAS library with a libref (a shortcut name), clears one or all librefs, lists the characteristics of a SAS library, concatenates SAS libraries, or concatenates SAS catalogs.

        <pre><code class="language-sas">LIBNAME adress98 'i:\\applicat\\pfb_safari\\menage98';
LIBNAME men 'c:';</code></pre>

        <h2>DATAL Statement</h2>

        <p>Dernière révision : 12 décembre 2022.</p>
    </div>`
};